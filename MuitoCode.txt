(o código a pôr no git bash era "git clone (link do repo)"
(depois era "git add .")
(depois foi "git commit -m 'Mudanças feitas'")
(e depois "git push")
(se quiser apagar um commit, copio o token do commit e faço "git revert (token)")
(depois clico no esc; dois pontos; wq; enter)
(para criar uma branch digo "git checkout -b (nome da branch)"; "git checkout main" para voltar para a branch do main)
("git merge main" para juntar a nossa branch com o main)

Olá Vila Real